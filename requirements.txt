# Runtime dependencies. Kept minimal per the brief — statistics use the stdlib
# `statistics` module, not numpy/scipy. Plotting libraries (matplotlib) will be added
# alongside the visualization bonus when that code actually lands.
pyyaml>=6.0
