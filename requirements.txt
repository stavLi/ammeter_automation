# Runtime dependencies. Kept minimal per the brief — statistics use the stdlib
# `statistics` module, not numpy/scipy.
pyyaml>=6.0

# Visualization bonus. matplotlib (which pulls numpy) is the only heavy-ish dependency; it
# is imported lazily in src/testing/viz.py so the core run, read-only queries, and the test
# suite don't pay for it unless plots are actually generated.
matplotlib>=3.5
