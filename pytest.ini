[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast tests with no real network (mocks/pure logic allowed)
    integration: tests that talk to a real emulator over a socket
filterwarnings =
    # Third-party noise: matplotlib calls deprecated pyparsing APIs. Not our code.
    ignore::pyparsing.PyparsingDeprecationWarning
